import { FC } from 'react';
import { useStyles } from './styles';

type PropsType = {};

export const FilmCard: FC<PropsType> = (props: PropsType) => {
  const classes = useStyles();

  return <div></div>;
};
